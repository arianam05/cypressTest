{
  "name": "cypressTest",
  "version": "1.0.0",
  "description": "cypress template for test",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "runOnChrome": "cypress run --browser chrome",
    "test:headless": "cypress run --browser chrome --headless",
    "test-report": "cypress run --reporter mochawesome"
  },
  "author": "ariana mendez",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/cypress": "^10.0.1",
    "cypress": "^13.7.3",
    "cypress-iframe": "^1.0.1",
    "cypress-mochawesome-reporter": "^3.8.2"
  },
  "dependencies": {
    "esbuild": "^0.16.16",
    "path": "^0.12.7"
  }
}
